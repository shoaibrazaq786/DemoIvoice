@model IEnumerable<ChartOfAccountViewModel>
@{ ViewData["Title"] = "Chart Of Accounts"; }
<div class="d-flex justify-content-between mb-4">
    <h4><i class="fas fa-sitemap"></i> Chart Of Accounts</h4>
    <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus"></i> Add Account</a>
</div>
<div class="card"><div class="card-body">
@await Html.PartialAsync("_AccountTree", Model)
</div></div>
